---
description: Implementation approved and finalized — write tests for it
---

The implementation is approved and finalized — tests may now be written.

If /finalize has not been run in this session, stop and propose running it first.

1. Decide what exactly to test: the public behavior of the changed code, not implementation details.
2. Propose the list of test cases, one line each, and wait for my OK.
3. Write the tests from the accepted list, following the existing test patterns in this project (find a nearby example).
4. Run the tests, get to a green run, and show the result.
